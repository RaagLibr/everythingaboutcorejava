package com.enfy.abstractioninterface;

public interface Inter1 {
	
	//interface not allow instance variable and non abstract method
	//it allow constant and abstract
	//we can achieve multiple inheritance 
	//interface does not allow instance variable , 
	//interface can achive multiple inheritance because it has it has method under it.

	public static final int a1=10;
	void mul(int a,int b);
	//inteface add abstract by default 
		//so method will be abstract by default

}
